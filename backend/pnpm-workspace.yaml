allowBuilds:
  better-sqlite3: true
  esbuild: true
# better-sqlite3 is a native module: without its build script it cannot load at
# runtime. esbuild (used by tsx) needs its platform binary.
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
